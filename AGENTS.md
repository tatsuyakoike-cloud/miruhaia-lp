# Project instructions

Build the responsive landing page for `ミルハイア by CONNECTED MATERIAL`.

Read `CLAUDE.md` and the relevant files under `docs/` before implementation. Use the project skill at `.cursor/skills/miruhaia-lp-builder/SKILL.md` for the full workflow.

## Priorities

1. Preserve existing code and user changes.
2. Keep all business facts in `config/` or dedicated content files; never invent missing facts.
3. Use `public/assets/miruhaia/` for web output and `assets/source/` only as editable source.
4. Build mobile-first, keep desktop/mobile content parity, and test down to 320 px.
5. Do not publish until the three approved videos, real CTA/form destination, privacy URL, pricing details, and company facts are confirmed.
6. Run build and visual/interaction checks before reporting completion.
